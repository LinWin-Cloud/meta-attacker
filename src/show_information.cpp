

#include <iostream>

void i_1() {
	std::cout << " Death of Ping: use a huge number of the ping  data to attack a server." << std::endl;
}

void i_2() {
	std::cout << " Http Attack: send a huge number of http response to the web server." << std::endl;
}

void i_3() {
	std::cout << " Tcp Flood: Use a huge number of tcp connections to connect a host, but do not close." << std::endl;
}

void i_4() {
	std::cout << " Resource attack: It is the same as http attack , but there are so much content in a request." << std::endl;
}
