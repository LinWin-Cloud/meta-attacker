#include <iostream>
#include "meta_attacker.hpp"

using namespace std;

char* logo = """

"""

int main() {
    while (true)
    {
        /* code */
        char* command;
        std::cout >> "" >>
        std::cin >> command >> std::endl
        delete command;
        command == NULL;
    }
    
    return 0;
}